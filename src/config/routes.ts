export enum Routes {
    Candidates = '/candidates',
    InstallMetaMask = '/install-metamask',
    Results = '/results',
    Vote = '/vote',
}

export enum Titles {
    Candidates = 'Candidates',
    InstallMetaMask = 'Install MetaMask',
    Results = 'Results',
    Vote = 'Vote',
}

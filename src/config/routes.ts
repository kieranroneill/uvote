export enum Routes {
    Candidates = '/candidates',
    Results = '/results',
    Vote = '/vote',
}

export enum Titles {
    Candidates = 'Candidates',
    Results = 'Results',
    Vote = 'Vote',
}

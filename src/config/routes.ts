export enum Routes {
    Candidates = '/candidates',
    InstallMetaMask = '/install-metamask',
    Results = '/results',
}

export enum Titles {
    Candidates = 'Candidates',
    InstallMetaMask = 'Install MetaMask',
    Results = 'Results',
}
